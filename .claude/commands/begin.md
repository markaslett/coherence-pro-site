Start a new session. Run the /begin protocol from CLAUDE.md Section 0.
Detect interface, detect session type, check lock, read brain files,
validate modules and capabilities, auto-sync kit if outdated,
sync issues, run proactive scan, output /status.
